<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage permissions - version specific -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".SportHubApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logoclean"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoround"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportHUB"
        tools:targetApi="31">

        <!-- File Provider for camera/gallery -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.sporthub.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Your activities here -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.login.GenderSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.profile.edit.EditNameActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.profile.edit.EditGenderActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.profile.edit.EditBirthDateActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.profile.edit.EditSportsActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.login.BirthDateSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.login.NameSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.login.FavoriteSportsSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.SportHUB" />
        <activity
            android:name=".ui.login.SignInActivity"
            android:exported="true"
            android:theme="@style/Theme.SportHUB.SignIn"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>